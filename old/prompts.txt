I need a html page that uses the entire screen and shows 3 columns side by side. First column should have 4 tables with 1 row each and filed name NOME, SENHA, DESTINO and PRIORIDADE. Second column should have one table with fields : POSIÇÃO, NOME, SENHA and PREVISÃO with multiple rows. The Third column should have one table with 2 columns: SALA and MÉDICO. Each of the tables data should come from a different api.